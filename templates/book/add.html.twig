{% extends 'base.html.twig' %}
{% block body %}
    {% if error is defined and error != false %}
        {{ error }}
    {% endif %}
    <div class="col-12">
        {{ form_start(form) }}
        {{ form_row(form.type) }}
        {{ form_end(form) }}
    </div>
{% endblock %}